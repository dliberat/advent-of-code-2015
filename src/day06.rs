use std::fs::File;
use std::io::{ Lines, BufReader };

pub(crate) fn solve(input: Lines<BufReader<File>>) {

    for line in input {
        todo!();
    }

    println!("Part 1: Total number of lit lights: {}", 0);
}
